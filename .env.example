# GreenPulse 环境变量样例。
# 密钥一律走环境变量(ENGINEERING_RULES §5);mock/合成 Provider 保证无密钥可跑 CI 与 demo。
# 以下均为可选:只有启用对应真实信号源时才需要。

# Electricity Maps API(可选;数据受商业条款约束,快照不得入库,见 docs/DATA_LICENSES.md)
# ELECTRICITY_MAPS_API_KEY=

# WattTime API(可选;不覆盖中国大陆)
# WATTTIME_USERNAME=
# WATTTIME_PASSWORD=
